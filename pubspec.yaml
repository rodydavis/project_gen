name: project_gen
description: A new Flutter package project.
version: 1.0.0
author:
homepage:

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  reflected_mustache: ^1.0.11
  freezed_annotation: any
  recase: ^3.0.0
  archive: ^2.0.13
  image_resizer: ^1.0.1
  template_gen:
    path: ../template_gen

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_version: ^2.0.0
  freezed: ^0.10.2
  settings_gen: any
  build_runner: ^1.7.1

dependency_overrides:
  image_resizer:
    git: https://github.com/rodydavis/image_resizer

flutter:

